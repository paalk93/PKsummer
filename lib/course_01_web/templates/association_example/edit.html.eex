<h2>Edit User</h2>

<%= render "form.html", Map.put(assigns, :action, association_example_path(@conn, :update, @user)) %>

<span><%= link "Back", to: association_example_path(@conn, :index) %></span>
